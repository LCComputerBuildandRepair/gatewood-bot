Drop your server logo here as logo.png (or logo.jpg).

It is used for three things:
  * the bot's avatar        -> npm run avatar
  * the Discord server icon -> /setup
  * the icon on every embed  -> automatic, from the bot's avatar

Square, at least 512x512, under 8 MB.
